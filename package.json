{
  "name": "configbuilder",
  "version": "1.0.0",
  "homepage": "https://bastionbot.org/configbuilder",
  "private": true,
  "dependencies": {
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom --verbose",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build -m \"Updated Config Builder Website\""
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
